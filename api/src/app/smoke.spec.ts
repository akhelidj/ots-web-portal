// Foundation smoke test — proves the Jest runner executes for the api project.
// Replace/expand with real tests; this only verifies the harness is wired up.
describe('api test harness', () => {
  it('runs a trivial assertion', () => {
    expect(1 + 1).toBe(2);
  });
});
